# 部署到 GitHub 和 Cloudflare 快速指南

## 📦 第一步：推送到 GitHub

### 1. 初始化 Git 仓库

在项目目录打开命令行：

```bash
cd c:\Users\XiaoL\Desktop\邮箱服务器
git init
git add .
git commit -m "Initial commit: Temp Mail by Kuro"
```

### 2. 创建 GitHub 仓库

1. 访问 [github.com](https://github.com)
2. 点击右上角 **+** → **New repository**
3. 填写信息：
   - Repository name: `temp-mail-service`（或您喜欢的名字）
   - Description: `临时邮箱服务 by Kuro`
   - 选择 **Public** 或 **Private**
   - **不要**勾选 "Add README" 或 ".gitignore"
4. 点击 **Create repository**

### 3. 推送代码

复制 GitHub 显示的命令，或使用：

```bash
git branch -M main
git remote add origin https://github.com/你的用户名/temp-mail-service.git
git push -u origin main
```

✅ 现在代码已经在 GitHub 上了！

---

## ☁️ 第二步：配置 Cloudflare

### 🔑 A. 创建 KV 命名空间

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 左侧菜单：**Workers & Pages** → **KV**
3. 点击 **Create a namespace**
4. 创建两个命名空间：
   - 名称：`temp-mail-emails` → 点击 **Add**
   - 名称：`temp-mail-domains` → 点击 **Add**
5. **记录下两个 Namespace ID**（类似：`abc123def456...`）

### 📝 B. 修改 wrangler.toml

打开 `workers/wrangler.toml`，填入您的 KV ID：

```toml
[[kv_namespaces]]
binding = "EMAILS_KV"
id = "粘贴-emails-的-ID"  ← 替换这里

[[kv_namespaces]]
binding = "DOMAINS_KV"
id = "粘贴-domains-的-ID"  ← 替换这里

[vars]
ADMIN_API_KEY = "设置一个强密码"  ← 改成您的密码
```

保存后推送到 GitHub：

```bash
git add workers/wrangler.toml
git commit -m "Update KV namespace IDs"
git push
```

### 🚀 C. 部署 Workers

安装 Wrangler CLI（首次使用）：

```bash
npm install -g wrangler
wrangler login
```

部署三个 Workers：

```bash
cd workers
wrangler deploy email-api.js --name email-api
wrangler deploy domain-admin-api.js --name domain-admin-api
wrangler deploy email-receiver.js --name email-receiver
```

**记录显示的 Worker URLs**

### 📧 D. 配置 Email Routing

1. Cloudflare Dashboard → 选择您的域名
2. 左侧：**Email** → **Email Routing**
3. 点击 **Get started** / **Enable**
4. 添加路由规则：
   - **Destination address**: `*@yourdomain.com`（或 `*@subdomain.yourdomain.com`）
   - **Action**: Send to a Worker
   - **Worker**: 选择 `email-receiver`
5. 点击 **Save**

### 🌐 E. 部署前端到 Cloudflare Pages

#### 方法 1：Dashboard 手动部署

1. **Workers & Pages** → **Create application**
2. 选择 **Pages** 标签
3. 点击 **Connect to Git**
4. 选择您的 GitHub 仓库
5. 配置构建设置：
   - **Project name**: temp-mail（或自定义）
   - **Production branch**: main
   - **Build command**: 留空
   - **Build output directory**: `/`
   - **Root directory**: `/`
6. 点击 **Save and Deploy**

#### 方法 2：使用 GitHub Actions 自动部署

1. 获取 Cloudflare API Token：

   - Dashboard → My Profile → **API Tokens**
   - **Create Token** → 使用 "Edit Cloudflare Workers" 模板
   - 复制生成的 Token

2. 获取 Account ID：

   - Dashboard → Workers & Pages
   - 右侧显示 **Account ID**，复制

3. 添加 GitHub Secrets：

   - GitHub 仓库 → **Settings** → **Secrets and variables** → **Actions**
   - 点击 **New repository secret**
   - 添加两个 secrets：
     - Name: `CLOUDFLARE_API_TOKEN`，Value: 粘贴 API Token
     - Name: `CLOUDFLARE_ACCOUNT_ID`，Value: 粘贴 Account ID

4. 推送代码即可自动部署：

```bash
git push
```

### 🔗 F. 配置自定义域名（可选）

1. Pages 项目设置 → **Custom domains**
2. **Set up a custom domain**
3. 输入域名（如：`mail.yourdomain.com`）
4. Cloudflare 会自动配置 DNS

---

## ⚙️ 第三步：初始化域名后缀

1. 访问管理后台：`https://你的域名/admin.html`
2. 使用 `wrangler.toml` 中设置的 API Key 登录
3. 添加邮箱域名后缀（必须是配置了 Email Routing 的域名）

---

## ✅ 完成检查清单

- [ ] 代码推送到 GitHub
- [ ] 创建了两个 KV 命名空间
- [ ] 更新了 wrangler.toml 配置
- [ ] 部署了三个 Workers
- [ ] 配置了 Email Routing
- [ ] 部署了前端到 Pages
- [ ] （可选）配置了自定义域名
- [ ] 在管理后台添加了域名后缀

---

## 🆘 常见问题

**Q: Worker 部署失败怎么办？**

- 检查 wrangler.toml 中的 KV ID 是否正确
- 确保已登录：`wrangler login`

**Q: Pages 部署后显示 404？**

- 检查 Root directory 是否设置为 `/`
- 确保 `temp-mail-index.html` 在仓库根目录

**Q: 邮件收不到？**

- 检查 Email Routing 是否启用
- 确认路由规则指向 `email-receiver` Worker
- 查看 Worker 日志：`wrangler tail email-receiver`

**Q: API 请求失败？**

- 更新 `app-main.js` 中的 `API_URL` 为您的 Worker URL
- 检查浏览器 Console 的错误信息

---

## 📞 需要帮助？

查看详细文档：

- [README.md](./README.md) - 项目完整说明
- [ADMIN_GUIDE.md](./ADMIN_GUIDE.md) - 管理员指南
- [CLOUDFLARE_DEPLOY.md](./CLOUDFLARE_DEPLOY.md) - 完整部署文档
